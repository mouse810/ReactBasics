import React from 'react'

const Input = () => {
     return (
          <div>
               <input style={{ border: '3px solid red', color: 'Blue' }} type='number' min={5} max={10} />
          </div>
     )
}

export default Input;